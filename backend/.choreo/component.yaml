schemaVersion: 1.0
type: application
name: backend

runtime: python3.10

build:
  command: uv sync

run:
  command: uvicorn main:app --host 0.0.0.0 --port 8000

container:
  ports:
    - 8000

env:
  - name: FRONTEND_URL
    value: "http://localhost:5173"
